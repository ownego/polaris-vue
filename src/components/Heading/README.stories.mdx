import { Meta, Story, Canvas, ArgsTable, Source, Preview } from '@storybook/addon-docs';
import dedent from 'ts-dedent';

import { Heading } from '@/polaris-vue';

# Heading

Headings are used as the titles of each major section of a page in the interface.
For example, card components generally use headings as their title.

<Meta
  title="Components / Titles and text / Heading"
  component={ Heading }
  argTypes={{
    element: {
      control: {
        type: 'select',
        options: ['h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'p'],
      },
      table: {
        type: {
          summary: "'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'p'",
        },
      }
    },
    default: {
      table: {
        disable: 'true',
      }
    },
  }}
/>

export const Template = (args) => ({
  components: { Heading },
  setup() { return { args }; },
  template: `
    <Heading v-bind="args">
      Online store dashboard
    </Heading>
  `,
});

<Canvas>
  <Story
    name="Heading"
    height="50px"
    parameters = {{
      docs: {
        source: {
          code: dedent`
            <Heading>Online store dashboard</Heading>
          `,
        },
      },
    }}
  >{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Heading" />
