import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { Link } from '@/polaris-vue';

<Meta
  title="Components / Navigation / Link"
  component={ Link }
  argTypes={{
    default: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { Link },
  template: `<Link v-bind="$props">fulfilling orders</Link>`,
});

# Link

Links take users to another place, and usually appear within or directly following a sentence.

##### For actions that aren’t related to navigation, use the [button component.](/docs/components-actions-button--button)

##### Use component with camel case for prevent conflict with `<link/>` element in html.


<Canvas>
  <Story name="Link">
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Link" />
