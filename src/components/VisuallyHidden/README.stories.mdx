import dedent from 'ts-dedent';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { VisuallyHidden } from '@/polaris-vue';

<Meta
  title="Components / Titles and text / Visually Hidden"
  component={ VisuallyHidden }
  argTypes={{
    content: {
      name: 'content',
      description: 'The content to be hidden visually',
      table: {
        type: {
          summary: 'slot'
        },
      },
    },
    default: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const Template = (args) => ({
  components: { VisuallyHidden },
  template: `
    <VisuallyHidden>
      <span>Hidden content</span>
    </VisuallyHidden>
    <p>Content passed down have been hide</p>
  `,
});

# Visually hidden

Use when an element needs to be available to assistive technology (for example, a screen reader) but otherwise hidden.

<Canvas>
  <Story
    name="Visually Hidden"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <VisuallyHidden>
              <span>Hidden content</span>
            </VisuallyHidden>
            <p>Content passed down have been hide</p>
          `,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Visually Hidden" />
