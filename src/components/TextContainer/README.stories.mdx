import { Meta, Story, Canvas, ArgsTable, Source, Preview } from '@storybook/addon-docs';
import dedent from 'ts-dedent';

import { TextContainer, Heading } from '@/polaris-vue';

<Meta
  title="Components / Titles and text / TextContainer"
  component={ TextContainer }
  argTypes={{
    spacing: {
      control: {
        type: 'select',
        options: ['loose', 'tight'],
      },
      table: {
        type: {
          summary: '"loose" | "tight"',
        },
      },
    },
    'default slot': {
      description: 'The content to render in the text container.',
      table: {
        category: 'slots'
      }
    },
    default: {
      table: {
        disable: 'true',
      }
    },
  }}
/>

# Text container

A text container is used to wrap text elements such as paragraphs, headings, and lists to give them vertical spacing.

<br/><br/>

### Default text container

Use this component for default vertical spacing.

export const defaultTextContainer = (args, { argTypes }) => ({
  components: { TextContainer, Heading },
  template: `
    <TextContainer>
      <Heading>Install the Shopify POS App</Heading>
      <p>
        Shopify POS is the easiest way to sell your products in person. Available
        for iPad, iPhone, and Android.
      </p>
    </TextContainer>
  `
});
defaultTextContainer.parameters = {
  docs: {
    source: false,
  },
}

<Canvas>
  <Story story={defaultTextContainer} name="Default text container"></Story>
</Canvas>

### Tight text container

Use the tight spacing option to relate content topics to each other.

export const tightTextContainer = (args, { argTypes }) => ({
  components: { TextContainer, Heading },
  template: `
    <TextContainer spacing="tight">
      <Heading>Install the Shopify POS App</Heading>
      <p>
        Shopify POS is the easiest way to sell your products in person. Available
        for iPad, iPhone, and Android.
      </p>
    </TextContainer>
  `
});
tightTextContainer.parameters = {
  docs: {
    source: false,
  },
}

<Canvas>
  <Story story={tightTextContainer} name="Tight text container"></Story>
</Canvas>

### Example

export const Template = (args, { argTypes }) => ({
  components: { TextContainer },
  props: Object.keys(argTypes),
  template: `
    <TextContainer v-bind="$props">
      <p>
        Lorem ipsum dolor sit amet, consectetur adipiscing elit.
        Donec a diam lectus. Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec consectetur ante hendrerit.
      </p>
      <h2>Heading</h2>
      <p>
        Lorem ipsum dolor sit amet, consectetur adipiscing elit.
        Donec a diam lectus. Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec consectetur ante hendrerit.
      </p>
      <ul>
        <li>List item</li>
        <li>List item</li>
        <li>List item</li>
      </ul>
    </TextContainer>
  `
});

<Canvas>
  <Story story={Template} name="Example" height="250px"></Story>
</Canvas>

<ArgsTable story="Example" />
