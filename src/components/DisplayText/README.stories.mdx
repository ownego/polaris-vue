import dedent from 'ts-dedent';
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { DisplayText } from '@/polaris-vue';

<Meta
  title="Components / Titles and text  / Display text"
  component={ DisplayText }
  argTypes={{
    element: {
      control: {
        type: 'text',
      },
      table: {
        type: {
          summary: 'Tag name'
        }
      }
    },
    default: {
      table: {
        disable: true,
      },
    },
    size: {
      options: ['small', 'medium', 'large', 'extraLarge'],
      control: {
        type: 'select',
      },
      table: {
        type: {
          summary: null,
          default: 'medium',
        },
      },
    },
  }}
/>

export const Template = (args) => ({
  components: { DisplayText },
  setup() { return { args }; },
  template: `<DisplayText v-bind="args">Good evening, Dominic.</DisplayText>`,
});

# Display Text

Display styles make a bold visual statement.
Use them to create impact when the main goal is visual storytelling.
For example, use display text to convince or reassure merchants such as in marketing content or to capture attention during onboarding.

<Canvas>
  <Story
    name="Display text"
    height="60px"
    args={{
      element: 'p',
    }}
    parameters={{
      docs: {
        source: {
          code: dedent`<DisplayText>Good evening, Dominic.</DisplayText>`,
        },
      }
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Display text" />
