import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { TextStyle } from "@/polaris-vue";
import dedent from "ts-dedent";

<Meta
  title="Components / Titles and text / Text Style"
  component={TextStyle}
  argTypes={{
    variation: {
      name: "variation",
      description: "Give text additional visual meaning",
      options: ["positive", "negative", "warning", "strong", "subdued", "code"],
      control: { type: "select" },
      table: {
        type: {
          summary: null,
        },
      },
    },
    default: {
      table: {
        disable: true,
      },
    },
  }}
/>

export const Template = (args, { argTypes }) => ({
  components: { TextStyle },
  setup() {
    return { args };
  },
  template: `<TextStyle v-bind="args"> Hello </TextStyle>`,
});

# Text Style

Text style enhances text with additional visual meaning. For example, using subdued text to de-emphasize it from its surrounding text.

<Canvas>
  <Story
    name="Text Style"
    height="60px"
    parameters={{
      docs: {
        source: {
          code: dedent`
            <TextStyle v-bind="props"> Hello </TextStyle>
          `,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Text Style" />
